apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "omar-ai-llm-proxy.fullname" . }}-config
  labels:
    {{- include "omar-ai-llm-proxy.labels" . | nindent 4 }}
data:
  litellm-config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}